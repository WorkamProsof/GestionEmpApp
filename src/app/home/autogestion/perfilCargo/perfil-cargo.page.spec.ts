import { TestBed } from '@angular/core/testing';
import { PerfilCargoPage } from './perfil-cargo.page';

describe('PerfilCargoPage', () => {
  let component: PerfilCargoPage;

  beforeEach(async () => {
    TestBed.createComponent(PerfilCargoPage);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
