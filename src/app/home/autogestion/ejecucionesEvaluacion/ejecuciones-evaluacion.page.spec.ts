import { TestBed } from '@angular/core/testing';
import { EjecucionesEvaluacionPage } from './ejecuciones-evaluacion.page';

describe('EjecucionesEvaluacionPage', () => {
  let component: EjecucionesEvaluacionPage;

  beforeEach(async () => {
    TestBed.createComponent(EjecucionesEvaluacionPage);
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
